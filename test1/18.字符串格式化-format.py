# 方法1 一般格式化方法.format()

# vars = '{}直下三千尺，疑是{}落九天'.format('飞流','银河')
#
# # 方法2 按照下标
#
# vars = '{0}直下三千尺，疑是{1}落九天'.format('飞流','银河')

# 方法3 关键字
# vars = '{a}直下三千尺，疑是{b}落九天'.format(a='飞流',b='银河')
# 方法4 集合
# vars = '{0[0]}直下三千尺，疑是{0[1]}落九天'.format(['飞流','银河'])
data = {'a': '飞流', 'b': '银河'}
# vars = '{a}直下三千尺，疑是{b}落九天'.format(**data)

# 方法5 f
# vars = f'{data["a"]}直下三千尺，疑是{data["b"]}落九天'
# print(vars)

# 方法6 位置参数
var1 = '圆周率是:{:.3f}'.format(3.1415926)
print(var1)